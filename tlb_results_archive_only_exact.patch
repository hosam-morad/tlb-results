diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..1e4b3ee
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1 @@
+generator/__pycache__/
diff --git a/Makefile b/Makefile
new file mode 100644
index 0000000..0f063b1
--- /dev/null
+++ b/Makefile
@@ -0,0 +1,4 @@
+PYTHON ?= python3
+.DEFAULT_GOAL := tlb-results
+
+include generator/module.mk
diff --git a/generator/README.txt b/generator/README.txt
index 342da38..75f3059 100644
--- a/generator/README.txt
+++ b/generator/README.txt
@@ -1,64 +1,57 @@
-SMT results HTML dashboard / GitHub Pages site
-================================================
+TLB results HTML dashboard / GitHub Pages site
+==============================================
 
-Inputs
-------
+Data inputs
+-----------
 
-1. Shared simulation:
-   simulations/smt_windowed/shared/lru/pi_1ms/<workload>/repeat0/simulator.csv
+The dashboard consumes experiment data only from the compact tlbsim archive:
 
-2. Uniform ownership simulation:
-   simulations/smt_windowed/uniform_ownership/lru/pi_5ms_dw3_oi10p/<workload>/repeat0/simulator.csv
+  ../tlbsim2/archive/hosam.morad/common_smt_workloads.py
+  ../tlbsim2/archive/hosam.morad/linear_models/smt/coefficients.csv
+  ../tlbsim2/archive/hosam.morad/linear_models/smt/training_data.csv
+  ../tlbsim2/archive/hosam.morad/results/smt/shared/lru/all_workloads.csv
+  ../tlbsim2/archive/hosam.morad/results/smt/uniform_ownership/lru/pi_5ms_dw3_oi10p/all_workloads.csv
 
-3. Directional linear mosmodels:
-   archive/hosam.morad/linear_models/smt/coefficients.csv
+The uniform result CSV contains:
 
-4. Archived mosmodel training points:
-   archive/hosam.morad/linear_models/smt/training_data.csv
+  benchmark,mpki,cpi,stlb_shared_percent,stlb_thread1_only_percent,stlb_thread2_only_percent
 
-5. Uniform STLB state log:
-   simulations/smt_windowed/uniform_ownership/lru/pi_5ms_dw3_oi10p/<workload>/repeat0/mode_selection.log
+The dashboard does not read raw simulations, mode_selection.log, weights, or
+analysis/windowed_uniform_speedup/speedup.csv.
 
-   The active state is read from previous_mode (with compatible aliases).
-   State ids: 0=Shared, 1=Thread1Only, 2=Thread2Only.
-
-No weights are read by the HTML analysis.
-The HTML analysis does not read analysis/windowed_uniform_speedup/speedup.csv.
+Benchmark display names are static presentation metadata kept in
+  generator/benchmark_names.py
+so the dashboard has no non-archive tlbsim file dependency.
 
 Generate
 --------
 
-Run:
+From the tlb-results repository run:
 
-  make analysis/smt_results_html
+  make
 
-The publishable website is generated under:
+Before rendering, make asks tlbsim to refresh the compact simulation archive:
 
-  analysis/smt_results_html/site/
+  make -C ../tlbsim2 archive/hosam.morad/results/smt
 
-with:
+The publishable website is generated directly in the tlb-results repository:
 
-  site/index.html
-  site/style.css
-  site/results.json
-  site/.nojekyll
-  site/figures/*.svg
-  site/figures/*.png
-  site/figures/*.pdf
+  index.html
+  style.css
+  results.json
+  .nojekyll
+  figures/*.svg
+  figures/*.png
+  figures/*.pdf
 
 The HTML embeds the SVG figures. Clicking a figure opens its PDF version.
 PNG copies are also generated for compatibility or use in email/slides.
 
-The site directory is self-contained and can be copied directly to a
-GitHub Pages repository.
-
-Simulation MPKI
----------------
+Simulation MPKI/CPI
+-------------------
 
-For each simulator.csv, the final cumulative row is used:
-
-  MPKI_thread1 = 1000 * L2/Thread1/Misses / Thread1/InstructionCount
-  MPKI_thread2 = 1000 * L2/Thread2/Misses / Thread2/InstructionCount
+MPKI and CPI are read directly from the archived shared and uniform
+all_workloads.csv files.
 
 Directional mosmodel
 --------------------
@@ -70,23 +63,22 @@ For workload A+B:
 
 For each focal benchmark:
 
-  CPI_shared  = A * MPKI_shared  + B
-  CPI_uniform = A * MPKI_uniform + B
-  speedup     = (CPI_shared / CPI_uniform - 1) * 100
+  speedup = (CPI_shared / CPI_uniform - 1) * 100
 
 Uniform STLB state percentages
 ------------------------------
 
-For each current workload with completed shared and uniform simulator.csv files,
-mode_selection.log is read when available. The percentage of valid previous_mode
-rows in each state is calculated as:
+The archive stores pair-level percentages of the active modes:
 
-  state_percent = 100 * state_row_count / valid_mode_row_count
+  stlb_shared_percent
+  stlb_thread1_only_percent
+  stlb_thread2_only_percent
 
-The per-benchmark Pair results table maps the raw thread states directionally:
+The dashboard maps them directionally:
 
   focal is thread1: Focal-only=Thread1Only, Co-runner-only=Thread2Only
   focal is thread2: Focal-only=Thread2Only, Co-runner-only=Thread1Only
 
-If mode_selection.log is missing or incomplete while live simulations are running,
-the pair remains in the dashboard and the STLB state cell is shown as an em dash.
+Clean generated output with:
+
+  make tlb-results/clean
diff --git a/generator/archive_results.py b/generator/archive_results.py
index 5b56dea..c8a785a 100644
--- a/generator/archive_results.py
+++ b/generator/archive_results.py
@@ -2,25 +2,84 @@ from __future__ import annotations
 
 import csv
 from collections import defaultdict, deque
+from dataclasses import dataclass
 from pathlib import Path
 
 
+STLB_COLUMNS = (
+    "stlb_shared_percent",
+    "stlb_thread1_only_percent",
+    "stlb_thread2_only_percent",
+)
+
+
+@dataclass(frozen=True)
+class ArchiveResult:
+    mpki: float
+    cpi: float
+    stlb_shared_percent: float | None = None
+    stlb_thread1_only_percent: float | None = None
+    stlb_thread2_only_percent: float | None = None
+
+    def directional_stlb_state(self, *, focal_thread: int) -> dict[str, float | int | None]:
+        if self.stlb_shared_percent is None:
+            return {
+                "stlb_shared_percent": None,
+                "stlb_focal_only_percent": None,
+                "stlb_corunner_only_percent": None,
+                "stlb_state_sample_count": None,
+            }
+
+        if focal_thread == 1:
+            focal = self.stlb_thread1_only_percent
+            corunner = self.stlb_thread2_only_percent
+        elif focal_thread == 2:
+            focal = self.stlb_thread2_only_percent
+            corunner = self.stlb_thread1_only_percent
+        else:
+            raise ValueError(f"focal_thread must be 1 or 2, got {focal_thread!r}")
+
+        return {
+            "stlb_shared_percent": self.stlb_shared_percent,
+            "stlb_focal_only_percent": focal,
+            "stlb_corunner_only_percent": corunner,
+            # The compact archive stores percentages rather than raw decision rows.
+            "stlb_state_sample_count": None,
+        }
+
+
 class ArchiveResults:
-    """Directional MPKI/CPI rows from an archived all_workloads.csv.
+    """Directional rows from an archived all_workloads.csv.
 
     Rows are kept in file order so self-pairs (A|A) can consume the two
     directional thread rows independently.
+
+    Shared results contain benchmark/mpki/cpi. Uniform-ownership results also
+    contain the three archived STLB ownership percentages.
     """
 
-    def __init__(self, path: Path) -> None:
+    def __init__(self, path: Path, *, require_stlb_state: bool = False) -> None:
         self.path = path
-        self._rows: dict[tuple[str, str], deque[tuple[float, float]]] = defaultdict(deque)
+        self._rows: dict[tuple[str, str], deque[ArchiveResult]] = defaultdict(deque)
 
         with path.open(newline="", encoding="utf-8") as handle:
             reader = csv.DictReader(handle)
-            if reader.fieldnames != ["benchmark", "mpki", "cpi"]:
+            fieldnames = reader.fieldnames or []
+            required = {"benchmark", "mpki", "cpi"}
+            missing = sorted(required - set(fieldnames))
+            if missing:
+                raise ValueError(f"{path}: missing required column(s): {', '.join(missing)}")
+
+            present_stlb = [column for column in STLB_COLUMNS if column in fieldnames]
+            if present_stlb and len(present_stlb) != len(STLB_COLUMNS):
+                missing_stlb = [column for column in STLB_COLUMNS if column not in fieldnames]
+                raise ValueError(
+                    f"{path}: incomplete STLB columns; missing {', '.join(missing_stlb)}"
+                )
+            has_stlb_state = len(present_stlb) == len(STLB_COLUMNS)
+            if require_stlb_state and not has_stlb_state:
                 raise ValueError(
-                    f"{path}: expected header benchmark,mpki,cpi; got {reader.fieldnames!r}"
+                    f"{path}: expected archived STLB columns: {', '.join(STLB_COLUMNS)}"
                 )
 
             for line_number, row in enumerate(reader, start=2):
@@ -33,13 +92,38 @@ class ArchiveResults:
                 try:
                     mpki = float(row["mpki"])
                     cpi = float(row["cpi"])
-                except ValueError as exc:
+                    if has_stlb_state:
+                        stlb_values = tuple(float(row[column]) for column in STLB_COLUMNS)
+                    else:
+                        stlb_values = (None, None, None)
+                except (TypeError, ValueError) as exc:
                     raise ValueError(
-                        f"{path}:{line_number}: invalid numeric MPKI/CPI"
+                        f"{path}:{line_number}: invalid numeric result"
                     ) from exc
-                self._rows[(parts[0], parts[1])].append((mpki, cpi))
 
-    def take(self, focal: str, corunner: str) -> tuple[float, float]:
+                if has_stlb_state:
+                    assert all(value is not None for value in stlb_values)
+                    if any(value < 0.0 or value > 100.0 for value in stlb_values):
+                        raise ValueError(
+                            f"{path}:{line_number}: STLB percentage outside [0, 100]"
+                        )
+                    total = sum(stlb_values)  # type: ignore[arg-type]
+                    if abs(total - 100.0) > 0.02:
+                        raise ValueError(
+                            f"{path}:{line_number}: STLB percentages sum to {total:.4f}, not 100"
+                        )
+
+                self._rows[(parts[0], parts[1])].append(
+                    ArchiveResult(
+                        mpki=mpki,
+                        cpi=cpi,
+                        stlb_shared_percent=stlb_values[0],
+                        stlb_thread1_only_percent=stlb_values[1],
+                        stlb_thread2_only_percent=stlb_values[2],
+                    )
+                )
+
+    def take(self, focal: str, corunner: str) -> ArchiveResult:
         key = (focal, corunner)
         values = self._rows.get(key)
         if not values:
diff --git a/generator/benchmark_names.py b/generator/benchmark_names.py
new file mode 100644
index 0000000..b7c3ddb
--- /dev/null
+++ b/generator/benchmark_names.py
@@ -0,0 +1,53 @@
+"""Benchmark display names used by the results dashboard.
+
+This is presentation metadata kept with the dashboard generator so tlb-results
+needs no non-archive files from the tlbsim repository.
+"""
+
+FULL_NAMES = {
+    "bwavess": "spec_cpu2017/603.bwaves_s",
+    "bwavesr": "spec_cpu2017/503.bwaves_r",
+    "bwaves": "spec_cpu2006/410.bwaves",
+    "fotos": "spec_cpu2017/649.fotonik3d_s",
+    "fft": "hpcc/single_fft-4GB",
+    "fotor": "spec_cpu2017/549.fotonik3d_r",
+    "cactusr": "spec_cpu2017/507.cactuBSSN_r",
+    "cactuss": "spec_cpu2017/607.cactuBSSN_s",
+    "gamess": "spec_cpu2006/416.gamess",
+    "mcf": "spec_cpu2006/429.mcf",
+    "xalan17": "spec_cpu2017/623.xalancbmk_s",
+    "bfs": "gapbs/bfs-urand-4GB",
+    "sssp": "gapbs/sssp-urand-4GB",
+    "pr": "gapbs/pr-urand-4GB",
+    "cc": "gapbs/cc-urand-4GB",
+    "bc": "gapbs/bc-urand-4GB",
+    "tc": "gapbs/tc-urand-4GB",
+    "pr_u1": "gapbs/pr-urand-1GB",
+    "sssp1": "gapbs/sssp-urand-1GB",
+    "cactus": "spec_cpu2006/436.cactusADM",
+    "bfs1": "gapbs/bfs-urand-1GB",
+    "bfs2": "gapbs/bfs-urand-2GB",
+    "omnet17": "spec_cpu2017/620.omnetpp_s",
+    "pr_k1": "gapbs/pr-kron-1GB",
+    "pr_k4": "gapbs/pr-kron-4GB",
+    "xalan06": "spec_cpu2006/483.xalancbmk",
+    "omnet06": "spec_cpu2006/471.omnetpp",
+    "graph1": "graph500-2.1/1GB",
+    "libqtm": "spec_cpu2006/462.libquantum",
+    "exch2": "spec_cpu2017/648.exchange2_s",
+    "rnd_acc_lcg": "hpcc/single_random_access_lcg-4GB",
+    "graph4": "graph500-2.1/4GB",
+    "sssp_k4": "gapbs/sssp-kron-4GB",
+    "rnd_acc": "hpcc/single_random_access-4GB",
+    "mcf17": "spec_cpu2017/605.mcf_s",
+    "astar": "spec_cpu2006/473.astar",
+    "gups": "gups/2GB",
+    "bc_k4": "gapbs/bc-kron-4GB",
+}
+
+
+def full_name(short: str) -> str:
+    try:
+        return FULL_NAMES[short]
+    except KeyError as exc:
+        raise KeyError(f"missing dashboard benchmark name for {short!r}") from exc
diff --git a/generator/collect.py b/generator/collect.py
index 8fae0e9..a715274 100644
--- a/generator/collect.py
+++ b/generator/collect.py
@@ -8,9 +8,9 @@ import sys
 from pathlib import Path
 from statistics import mean, median
 
-from archive_results import ArchiveResults
+from archive_results import ArchiveResults, ArchiveResult
 from mosmodel import load_coefficients, load_training_data, speedup
-from stlb_state import directional_stlb_state, read_stlb_state
+from benchmark_names import full_name
 
 
 def parse_args() -> argparse.Namespace:
@@ -25,10 +25,6 @@ def parse_args() -> argparse.Namespace:
         "--wptlb-results",
         default="archive/hosam.morad/results/smt/uniform_ownership/lru/pi_5ms_dw3_oi10p/all_workloads.csv",
     )
-    parser.add_argument(
-        "--wptlb-state-root",
-        default="simulations/smt_windowed/uniform_ownership/lru/pi_5ms_dw3_oi10p",
-    )
     parser.add_argument(
         "--coefficients-csv",
         default="archive/hosam.morad/linear_models/smt/coefficients.csv",
@@ -37,11 +33,6 @@ def parse_args() -> argparse.Namespace:
         "--training-data-csv",
         default="archive/hosam.morad/linear_models/smt/training_data.csv",
     )
-    parser.add_argument(
-        "--benchmark-dictionary",
-        default="./benchmark_dictionary.py",
-    )
-    parser.add_argument("--repeat", default="repeat0")
     parser.add_argument(
         "--archive-root",
         default="archive/hosam.morad",
@@ -92,21 +83,6 @@ def split_pair(workload: str) -> tuple[str, str]:
     return parts[0], parts[1]
 
 
-def full_name(short: str, dictionary: Path, cache: dict[str, str]) -> str:
-    if short not in cache:
-        proc = subprocess.run(
-            [sys.executable, str(dictionary), "--short_to_full", short],
-            check=True,
-            text=True,
-            capture_output=True,
-        )
-        value = proc.stdout.strip()
-        if not value:
-            raise ValueError(f"benchmark dictionary returned empty name for {short!r}")
-        cache[short] = value
-    return cache[short]
-
-
 def directional_lookup(
     mapping,
     focal_short: str,
@@ -134,15 +110,13 @@ def build_side(
     uniform_mpki: float,
     shared_cpi: float,
     uniform_cpi: float,
-    dictionary: Path,
-    name_cache: dict[str, str],
     coefficients,
     training_data,
-    stlb_state,
+    uniform_result: ArchiveResult,
     focal_thread: int,
 ) -> dict:
-    focal_full = full_name(focal_short, dictionary, name_cache)
-    corunner_full = full_name(corunner_short, dictionary, name_cache)
+    focal_full = full_name(focal_short)
+    corunner_full = full_name(corunner_short)
 
     a, b = directional_lookup(
         coefficients,
@@ -176,7 +150,7 @@ def build_side(
         "model_b": b,
         "mosmodel_points": points,
     }
-    result.update(directional_stlb_state(stlb_state, focal_thread=focal_thread))
+    result.update(uniform_result.directional_stlb_state(focal_thread=focal_thread))
     return result
 
 
@@ -185,23 +159,20 @@ def main() -> int:
 
     baseline_results_csv = Path(args.baseline_results)
     wptlb_results_csv = Path(args.wptlb_results)
-    wptlb_state_root = Path(args.wptlb_state_root)
     coefficients_csv = Path(args.coefficients_csv)
     training_data_csv = Path(args.training_data_csv)
-    dictionary = Path(args.benchmark_dictionary).resolve()
     archive_root = Path(args.archive_root)
     workloads_script = Path(args.workloads_script)
 
     coefficients = load_coefficients(coefficients_csv)
     training_data = load_training_data(training_data_csv)
     baseline_results = ArchiveResults(baseline_results_csv)
-    wptlb_results = ArchiveResults(wptlb_results_csv)
+    wptlb_results = ArchiveResults(wptlb_results_csv, require_stlb_state=True)
     workloads = load_workloads(
         explicit_workloads=args.workloads,
         workloads_script=workloads_script,
         archive_root=archive_root,
     )
-    name_cache: dict[str, str] = {}
 
     pair_results: list[dict] = []
     directional_results: list[dict] = []
@@ -210,45 +181,38 @@ def main() -> int:
     for workload in workloads:
         benchmark1, benchmark2 = split_pair(workload)
 
-        wptlb_mode_log = wptlb_state_root / workload / args.repeat / "mode_selection.log"
         try:
-            shared_mpki1, shared_cpi1 = baseline_results.take(benchmark1, benchmark2)
-            shared_mpki2, shared_cpi2 = baseline_results.take(benchmark2, benchmark1)
-            uniform_mpki1, uniform_cpi1 = wptlb_results.take(benchmark1, benchmark2)
-            uniform_mpki2, uniform_cpi2 = wptlb_results.take(benchmark2, benchmark1)
+            shared1 = baseline_results.take(benchmark1, benchmark2)
+            shared2 = baseline_results.take(benchmark2, benchmark1)
+            uniform1 = wptlb_results.take(benchmark1, benchmark2)
+            uniform2 = wptlb_results.take(benchmark2, benchmark1)
         except KeyError as exc:
             skipped_workloads.append(workload)
             print(f"Skipping {workload}: {exc}", file=sys.stderr)
             continue
 
-        stlb_state = read_stlb_state(wptlb_mode_log)
-
         side1 = build_side(
             focal_short=benchmark1,
             corunner_short=benchmark2,
-            shared_mpki=shared_mpki1,
-            uniform_mpki=uniform_mpki1,
-            shared_cpi=shared_cpi1,
-            uniform_cpi=uniform_cpi1,
-            dictionary=dictionary,
-            name_cache=name_cache,
+            shared_mpki=shared1.mpki,
+            uniform_mpki=uniform1.mpki,
+            shared_cpi=shared1.cpi,
+            uniform_cpi=uniform1.cpi,
             coefficients=coefficients,
             training_data=training_data,
-            stlb_state=stlb_state,
+            uniform_result=uniform1,
             focal_thread=1,
         )
         side2 = build_side(
             focal_short=benchmark2,
             corunner_short=benchmark1,
-            shared_mpki=shared_mpki2,
-            uniform_mpki=uniform_mpki2,
-            shared_cpi=shared_cpi2,
-            uniform_cpi=uniform_cpi2,
-            dictionary=dictionary,
-            name_cache=name_cache,
+            shared_mpki=shared2.mpki,
+            uniform_mpki=uniform2.mpki,
+            shared_cpi=shared2.cpi,
+            uniform_cpi=uniform2.cpi,
             coefficients=coefficients,
             training_data=training_data,
-            stlb_state=stlb_state,
+            uniform_result=uniform2,
             focal_thread=2,
         )
 
@@ -264,7 +228,6 @@ def main() -> int:
                 "benchmark2_full": side2["focal_full"],
                 "baseline_results_csv": str(baseline_results_csv),
                 "wptlb_results_csv": str(wptlb_results_csv),
-                "wptlb_mode_log": str(wptlb_mode_log),
                 "side1": side1,
                 "side2": side2,
             }
@@ -316,8 +279,6 @@ def main() -> int:
         "inputs": {
             "baseline_results_csv": str(baseline_results_csv),
             "wptlb_results_csv": str(wptlb_results_csv),
-            "wptlb_state_root": str(wptlb_state_root),
-            "repeat": args.repeat,
             "coefficients_csv": str(coefficients_csv),
             "training_data_csv": str(training_data_csv),
             "archive_root": str(archive_root),
diff --git a/generator/module.mk b/generator/module.mk
index bf04d86..960c8de 100644
--- a/generator/module.mk
+++ b/generator/module.mk
@@ -1,14 +1,14 @@
-##### SMT results HTML dashboard / GitHub Pages site
-smt_results_html_dir := analysis/smt_results_html
-smt_results_html_site_dir := $(smt_results_html_dir)/site
-smt_results_html_json := $(smt_results_html_site_dir)/results.json
-smt_results_html_html := $(smt_results_html_site_dir)/index.html
+##### TLB results dashboard / GitHub Pages site
+smt_results_html_dir := generator
+smt_results_html_site_dir := .
+smt_results_html_json := results.json
+smt_results_html_html := index.html
 
-##### inputs
-smt_results_html_archive_root ?= archive/hosam.morad
+##### tlbsim archive inputs
+smt_results_html_tlbsim_root ?= ../tlbsim2
+smt_results_html_archive_root ?= $(smt_results_html_tlbsim_root)/archive/hosam.morad
 smt_results_html_workloads_script ?= \
 	$(smt_results_html_archive_root)/common_smt_workloads.py
-smt_results_html_repeat ?= repeat0
 
 smt_results_html_baseline_results ?= \
 	$(smt_results_html_archive_root)/results/smt/shared/lru/all_workloads.csv
@@ -16,9 +16,6 @@ smt_results_html_baseline_results ?= \
 smt_results_html_wptlb_results ?= \
 	$(smt_results_html_archive_root)/results/smt/uniform_ownership/lru/pi_5ms_dw3_oi10p/all_workloads.csv
 
-smt_results_html_wptlb_state_root ?= \
-	simulations/smt_windowed/uniform_ownership/lru/pi_5ms_dw3_oi10p
-
 smt_results_html_coefficients_csv ?= \
 	$(smt_results_html_archive_root)/linear_models/smt/coefficients.csv
 
@@ -26,34 +23,31 @@ smt_results_html_training_data_csv ?= \
 	$(smt_results_html_archive_root)/linear_models/smt/training_data.csv
 
 ##### targets
-.PHONY: analysis/smt_results_html analysis/smt_results_html/clean smt_results_html_force
+.PHONY: tlb-results tlb-results/archive tlb-results/clean smt_results_html_force
+
+tlb-results: $(smt_results_html_html)
 
-analysis/smt_results_html: $(smt_results_html_html)
+# tlbsim owns raw simulation -> archive conversion. The dashboard only asks
+# tlbsim to refresh that compact archive before reading it.
+tlb-results/archive:
+	$(MAKE) -C "$(smt_results_html_tlbsim_root)" archive/hosam.morad/results/smt
 
 smt_results_html_force:
 
 $(smt_results_html_json): \
 	smt_results_html_force \
+	tlb-results/archive \
 	$(smt_results_html_dir)/collect.py \
 	$(smt_results_html_dir)/archive_results.py \
-	$(smt_results_html_dir)/stlb_state.py \
-	$(smt_results_html_dir)/mosmodel.py \
-	$(smt_results_html_workloads_script) \
-	$(smt_results_html_coefficients_csv) \
-	$(smt_results_html_training_data_csv) \
-	$(smt_results_html_baseline_results) \
-	$(smt_results_html_wptlb_results)
-	mkdir -p $(smt_results_html_site_dir)
+	$(smt_results_html_dir)/benchmark_names.py \
+	$(smt_results_html_dir)/mosmodel.py
 	$(PYTHON) $(smt_results_html_dir)/collect.py \
 		--baseline-results "$(smt_results_html_baseline_results)" \
 		--wptlb-results "$(smt_results_html_wptlb_results)" \
-		--wptlb-state-root "$(smt_results_html_wptlb_state_root)" \
 		--coefficients-csv "$(smt_results_html_coefficients_csv)" \
 		--training-data-csv "$(smt_results_html_training_data_csv)" \
-		--benchmark-dictionary "$(CURDIR)/benchmark_dictionary.py" \
 		--archive-root "$(smt_results_html_archive_root)" \
 		--workloads-script "$(smt_results_html_workloads_script)" \
-		--repeat "$(smt_results_html_repeat)" \
 		--output "$@"
 
 $(smt_results_html_html): \
@@ -65,7 +59,7 @@ $(smt_results_html_html): \
 	$(PYTHON) $(smt_results_html_dir)/render.py \
 		--input "$(smt_results_html_json)" \
 		--output "$@" \
-		--figures-dir "$(smt_results_html_site_dir)/figures"
+		--figures-dir "figures"
 
-analysis/smt_results_html/clean:
-	rm -rf $(smt_results_html_site_dir)
+tlb-results/clean:
+	rm -rf results.json index.html style.css .nojekyll figures
diff --git a/generator/stlb_state.py b/generator/stlb_state.py
deleted file mode 100644
index f185ce6..0000000
--- a/generator/stlb_state.py
+++ /dev/null
@@ -1,154 +0,0 @@
-from __future__ import annotations
-
-import csv
-from pathlib import Path
-
-
-MODE_COLUMN_CANDIDATES = (
-    "previous_mode",
-    "mode",
-    "real_mode",
-    "current_mode",
-)
-
-MODE_NAMES = {
-    0: "Shared",
-    1: "Thread1Only",
-    2: "Thread2Only",
-}
-
-_MODE_TEXT_TO_ID = {
-    "0": 0,
-    "shared": 0,
-    "1": 1,
-    "thread1only": 1,
-    "thread1_only": 1,
-    "thread1-only": 1,
-    "2": 2,
-    "thread2only": 2,
-    "thread2_only": 2,
-    "thread2-only": 2,
-}
-
-
-def _normalize_mode(value: str) -> int | None:
-    text = str(value).strip()
-    if not text:
-        return None
-
-    try:
-        numeric = int(float(text))
-    except ValueError:
-        numeric = None
-
-    if numeric in MODE_NAMES:
-        return numeric
-
-    return _MODE_TEXT_TO_ID.get(text.lower())
-
-
-def _rows_from_text(text: str) -> tuple[list[str], list[dict[str, str]]]:
-    lines = [line for line in text.splitlines() if line.strip()]
-    if not lines:
-        return [], []
-
-    sample = "\n".join(lines[:20])
-    try:
-        dialect = csv.Sniffer().sniff(sample, delimiters=",;\t")
-    except csv.Error:
-        dialect = None
-
-    if dialect is not None:
-        reader = csv.DictReader(lines, dialect=dialect)
-        fieldnames = [str(name).strip() for name in (reader.fieldnames or [])]
-        rows = [
-            {str(key).strip(): str(value).strip() for key, value in row.items() if key is not None}
-            for row in reader
-        ]
-        return fieldnames, rows
-
-    # Fallback for whitespace-delimited logs.
-    fieldnames = lines[0].split()
-    rows: list[dict[str, str]] = []
-    for line in lines[1:]:
-        values = line.split()
-        if len(values) < len(fieldnames):
-            continue
-        rows.append(dict(zip(fieldnames, values)))
-    return fieldnames, rows
-
-
-def read_stlb_state(path: Path) -> dict[str, float | int] | None:
-    """Return percentages of the actually active STLB modes.
-
-    The simulator's mode_selection.log records one row per decision interval.
-    We use previous_mode (or a compatible alias) because it describes the mode
-    that was active during the interval being evaluated.
-    """
-    if not path.is_file():
-        return None
-
-    try:
-        text = path.read_text(encoding="utf-8")
-    except OSError:
-        return None
-
-    fieldnames, rows = _rows_from_text(text)
-    if not fieldnames or not rows:
-        return None
-
-    mode_column = next(
-        (candidate for candidate in MODE_COLUMN_CANDIDATES if candidate in fieldnames),
-        None,
-    )
-    if mode_column is None:
-        return None
-
-    counts = {mode: 0 for mode in MODE_NAMES}
-    valid_rows = 0
-    for row in rows:
-        mode = _normalize_mode(row.get(mode_column, ""))
-        if mode is None:
-            continue
-        counts[mode] += 1
-        valid_rows += 1
-
-    if valid_rows == 0:
-        return None
-
-    return {
-        "shared_percent": 100.0 * counts[0] / valid_rows,
-        "thread1_only_percent": 100.0 * counts[1] / valid_rows,
-        "thread2_only_percent": 100.0 * counts[2] / valid_rows,
-        "sample_count": valid_rows,
-    }
-
-
-def directional_stlb_state(
-    state: dict[str, float | int] | None,
-    *,
-    focal_thread: int,
-) -> dict[str, float | int | None]:
-    if state is None:
-        return {
-            "stlb_shared_percent": None,
-            "stlb_focal_only_percent": None,
-            "stlb_corunner_only_percent": None,
-            "stlb_state_sample_count": None,
-        }
-
-    if focal_thread == 1:
-        focal_key = "thread1_only_percent"
-        corunner_key = "thread2_only_percent"
-    elif focal_thread == 2:
-        focal_key = "thread2_only_percent"
-        corunner_key = "thread1_only_percent"
-    else:
-        raise ValueError(f"focal_thread must be 1 or 2, got {focal_thread!r}")
-
-    return {
-        "stlb_shared_percent": float(state["shared_percent"]),
-        "stlb_focal_only_percent": float(state[focal_key]),
-        "stlb_corunner_only_percent": float(state[corunner_key]),
-        "stlb_state_sample_count": int(state["sample_count"]),
-    }
